<%= form_with(model: items_category, local: true) do |form| %>
  <% if items_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(items_category.errors.count, "error") %> prohibited this items_category from being saved:</h2>

      <ul>
      <% items_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id, id: :items_category_item_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :items_category_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
